/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package br.com.atsinformatica.utils;


import java.io.File;
import java.io.FileInputStream;
import java.io.FileOutputStream;
import org.apache.log4j.Logger;
/**
 *
 * @author AlexsanderPimenta
 */
public class FileUpload {
    
    /**
     * Faz upload de um arquivo em um servidor.
     *
     * @param url
     * @param host
     * @param senha
     */
//    public void upload(String url, String host, String dir, String usuario, String senha) {
//        FileInputStream fs = null;
//        try {
//            Logger.getLogger(FileUpload.class).info("Iniciando upload de arquivo..");
//            //diretorio ftp
//           // client.connect(host);            
//           // boolean logado = client.login(usuario, senha);
//            //if(logado){
//                Logger.getLogger(FileUpload.class).info("Logado no servidor com sucesso...");
//                fs = new FileInputStream(new File(url)); 
//                
//              //  fo = new FileOutputStream
//             //   boolean ok = client.storeFile(dir, fs);                
//              //  if(ok){
//                   Logger.getLogger(FileUpload.class).info("Upload de "+dir+" efetuado com sucesso!");
//                //}else{
//                   Logger.getLogger(FileUpload.class).info("Falha ao efetuar upload do arquivo."); 
//               // }
//            }
//            //client.disconnect();
//        } catch (Exception ex) {
//            Logger.getLogger(FileUpload.class).info("Erro ao efetuar upload do arquivo: "+ex);
//        }
//    }
    
}
